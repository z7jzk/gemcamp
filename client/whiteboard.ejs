<!doctype html>
<html lang="en" ng-app>
  <head>
    <title><%= title %></title>
    <%- include('head') %>
  </head>
  <body>
    <div class="container">
      <%- include('header') %>
      <!-- start body here -->
      <div class="page-header">
        <h1 style="">Welcome to The Whiteboard</h1>
      </div>
      <br>
      <div>
          <h3 id="sname" style="display: none;"></h3>
      </div>
      <br>
      <a class="btn btn-default" href="/chat.html">Change Chat Details</a>
      <br/>
      <br/>
      <br/>
      <form id="message-form" class="form-horizontal">
        <div class="form-group">
          <div class="row">
            <div class="col-sm-10">
              <input id = "m" type="text" name="message" class="form-control">
            </div>
            <div class="col-sm-2">
              <input type="submit" value="submit" class="btn btn-primary">
            </div>
          </div>
        </div>
      </form>
      <div class="messages">
        <!-- messages go here -->
      </div>
    </div>
    <!-- end body here -->
    </div>
    <%- include('footer') %>
    <%- include('scripts') %>
    <script>
      function loadChats() {
        var momentTimestamp = moment().utcOffset("-05:00").format('h:mm a');
        var message_text = $('#m').val();
        var send_name = $.ajax({
          type: 'get'
          url: "/wbitems",
          remote: true
        }).done(function( data ) {
            //console.log(data);
            //$('#name-modal').modal('hide');
        }).fail(function (jqXHR, textStatus) {
            alert('Chats Returned!');
        });
      }
      
      function sendChats() {
        var momentTimestamp = moment().utcOffset("-05:00").format('h:mm a');
        var message_text = $('#m').val();
        var send_name = $.ajax({
          type: 'post'
          url: "/wbitems",
          remote: true,
          data: {name: user_name, mtext: message_text, time: momentTimestamp},
          dataType: 'json'
        }).done(function( data ) {
            //console.log(data);
            //$('#name-modal').modal('hide');
        }).fail(function (jqXHR, textStatus) {
            alert('Chat Posted!');
        });
      }
      
      loadChats();
    </script>
  </body>
</html>